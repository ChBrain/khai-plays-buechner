---
"@chbrain/khai-plays-buechner": patch
---

data: restore the German `declared` originals on Woyzeck elements that shipped English in `declared` (`declared: "The Razor"` -> `Das Rasiermesser`, `The Doctor` -> `Der Doktor`, `The Pond` -> `Der Teich`, and 16 more). `declared` is the source-language name the book reads and the stage shows under Original Language; it must hold German, not the English display name. Only `declared` changes; the English `title`, ids, links, and prose are untouched. Marie, Franz Woyzeck, and the plots/plan already carried correct German.
