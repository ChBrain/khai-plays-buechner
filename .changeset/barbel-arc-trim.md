---
"@chbrain/khai-plays-buechner": patch
---

Trim The Barbel Memoir's arc to within the registry's 120-character limit, so
`khai-tests registry build` heals `registry.json` from source without overflow.
